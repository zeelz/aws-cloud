version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - npm run install
  build:
    commands:
      - npm run build

artifacts:
  base-directory: build
  files:
    - "**/*"
